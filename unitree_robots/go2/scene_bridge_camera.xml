<mujoco model="go2 bridge camera scene">
  <include file="go2_bridge_camera.xml"/>

  <statistic center="2.0 0 0.5" extent="6.0"/>

  <visual>
    <headlight diffuse="0.6 0.6 0.6" ambient="0.3 0.3 0.3" specular="0 0 0"/>
    <rgba haze="0.15 0.25 0.35 1"/>
    <global azimuth="-130" elevation="-20"/>
  </visual>

  <asset>
    <texture type="skybox" builtin="gradient" rgb1="0.3 0.5 0.7" rgb2="0 0 0" width="512" height="3072"/>
    <texture type="2d" name="groundplane" builtin="checker" mark="edge" rgb1="0.2 0.3 0.4" rgb2="0.1 0.2 0.3"
      markrgb="0.8 0.8 0.8" width="300" height="300"/>
    <material name="groundplane" texture="groundplane" texuniform="true" texrepeat="5 5" reflectance="0.2"/>
  </asset>

  <worldbody>
    <light pos="0 0 3.0" dir="0 0 -1" directional="true"/>

    <!-- Low catch floor below the bridge.  It is intentionally below the camera
      prior's height filter, so the bridge remains the local walkable surface. -->
    <geom name="floor" pos="2.0 0 -0.05" size="0 0 0.05" type="plane" material="groundplane"/>

    <!-- Straight bridge copied from bridge_mjlab BridgeTerrainCfg in deploy MJCF.
      bridge_mjlab v3/v4 play defaults:
        platform_a: center x=-1.0, half-size=(1.0, 1.5, 0.05), top z=0.55
        bridge:    center x= 2.0, half-size=(2.0, 0.10, 0.05), top z=0.55
        platform_b: center x= 5.5, half-size=(1.5, 1.5, 0.05), top z=0.55
      The Go2 starts on platform_a at x=-1.3, z=1.0 in go2_bridge_camera.xml,
      matching bridge_mjlab reset_base x=(0.20, 0.30) relative to terrain origin
      x=-1.5 and origin z = height + 0.5. -->
    <geom name="platform_a" type="box" size="1.0 1.5 0.05" pos="-1.0 0 0.5" rgba="0.3 0.3 0.3 1"/>
    <geom name="straight_bridge" type="box" size="2.0 0.10 0.05" pos="2.0 0 0.5" rgba="0.5 0.4 0.3 1"/>
    <geom name="platform_b" type="box" size="1.5 1.5 0.05" pos="5.5 0 0.5" rgba="0.3 0.3 0.3 1"/>

    <!-- Thin visual centerline/goal markers; contype=0/conaffinity=0 keeps them
      out of contacts.  They are group 3 so downstream perception can filter them
      if needed. -->
    <geom name="bridge_centerline_vis" type="box" size="3.75 0.01 0.005" pos="2.25 0 0.565"
      rgba="1.0 0.1 0.1 1" contype="0" conaffinity="0" group="3"/>
    <geom name="bridge_goal_vis" type="sphere" size="0.06" pos="6.5 0 0.62"
      rgba="0.1 0.8 1.0 1" contype="0" conaffinity="0" group="3"/>
  </worldbody>
</mujoco>
